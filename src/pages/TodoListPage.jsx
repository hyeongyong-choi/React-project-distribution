import TodoList from 'components/TodoList';
import Layout from 'layout/Layout';

const TodoListPage = () => {
  return (
    <div>
      <Layout>
      <TodoList />
      </Layout>
    </div>
  );
};

export default TodoListPage;
